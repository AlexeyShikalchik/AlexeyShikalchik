'use strict'

const arr = [ 5, 7, 
    [ 4, [2], 8, [1,3], 2 ], 
    [ 9, [] ], 
    1, 8
  ];

function treeSum(getArr) {
    for (var i = 0; i <= getArr.length-1; i++){
        if (isArray(getArr[key])){
            
        }
    }
}

treeSum(arr);